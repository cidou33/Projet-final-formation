<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="{{ asset('assets/css/style.css') }}">
    <link rel="stylesheet" href="{{ asset('assets/css/base.css') }}">

    <title>Nouvel article</title>
</head>
<body>
    {% include 'commons/header.html.twig' %}
    <main class="mainAddNews {% if is_granted('ROLE_ADMIN') %}adminMainHomePage{% else %}mainHomePage{% endif %} mainTranscriptions">
        {% block body %}

            {{ form(newsForm) }}
        {% endblock %}
    </main>
    {% include 'commons/footer.html.twig' %}
    {% if is_granted('ROLE_ADMIN') %}
    <script async src="{{ asset('assets/js/scriptAdmin.js') }}"></script>
    {% endif %}
    <script async src="{{ asset('assets/js/scriptUser.js') }}"></script>
</body>
</html>