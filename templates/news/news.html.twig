<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="{{ asset('assets/css/style.css') }}">
    <title>Les News</title>
</head>
<body>
{% block body %}
    {% include 'commons/header.html.twig' %}
    <main>
        <h1>Les News</h1>
        <p>blaaaaaaaaaaaaa</p>
        <section>
            {% for new in news %}
                {%  if new.active == true %}
                <article>
                    <div>
                        <h2>{{ new.title }}</h2>
                        <span>Publié le {{ new.createDate|date('d-m-Y') }}</span>
                        <img src="{{ asset('assets/data/img/uploadNews/' ~ new.imgNews) }}" alt="{{ new.imgAlt }}">
                    </div>
                    <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Commodi dolorum impedit modi molestiae nam quae quidem quis veniam? At dolorum ducimus eius illo molestiae nesciunt omnis pariatur quidem saepe voluptate.Deleniti enim nihil possimus quas quo. Commodi consequuntur culpa delectus fugiat id ipsam laborum, nihil placeat qui tempora unde voluptates. Blanditiis cumque nemo nisi quia quos reiciendis reprehenderit, sint! Maiores.</p>
                </article>
                    {% if is_granted('ROLE_ADMIN') %}
                        <div>
                            <a href="{{ path('deleteNews', {'id':new.id}) }}">supprimer</a>
                            <a href="{{ path('modifyStatusNews', {'id':new.id}) }}">activer</a>
                            <a href="{{ path('updateNews', {'id':new.id}) }}">modifier</a>
                        </div>
                    {% endif%}
                {% endif %}
            {% endfor %}
            {% if is_granted('ROLE_ADMIN') %}
                <h2>Les news inactives</h2>
                {% for new in news %}
                    {%  if new.active == false %}
                        <article>
                            <div>
                                <h2>{{ new.title }}</h2>
                                <span>Publié le {{ new.createDate|date('d-m-Y') }}</span>
                                <img src="{{ asset('assets/data/img/uploadNews/' ~ new.imgNews) }}" alt="{{ new.imgAlt }}">
                            </div>
                            <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Commodi dolorum impedit modi molestiae nam quae quidem quis veniam? At dolorum ducimus eius illo molestiae nesciunt omnis pariatur quidem saepe voluptate.Deleniti enim nihil possimus quas quo. Commodi consequuntur culpa delectus fugiat id ipsam laborum, nihil placeat qui tempora unde voluptates. Blanditiis cumque nemo nisi quia quos reiciendis reprehenderit, sint! Maiores.</p>
                        </article>

                        {% if is_granted('ROLE_ADMIN') %}
                            <div>
                                <a href="{{ path('deleteNews', {'id':new.id}) }}">supprimer</a>
                                <a href="{{ path('modifyStatusNews', {'id':new.id}) }}">activer</a>
                                <a href="{{ path('updateNews', {'id':new.id}) }}">modifier</a>
                            </div>
                        {% endif%}
                    {% endif %}
                {% endfor %}
            {% endif %}
        </section>
    </main>
{% endblock %}
{% include 'commons/footer.html.twig' %}
</body>
</html>